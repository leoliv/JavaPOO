package pacotemain;

/*  Crie uma classe de exceção que estenda de Exception chamada de ExcecaoAlfabeto.
    
    Crie um programa Java com um método que receba uma String de parâmetro chamado validarLetrasAlfabeto. Esse método deve verificar se cada um dos caracteres da String é uma letra do alfabeto, se for encontrado algum caractere especial, devemos lançar uma exceção do tipo criado no exercício anterior.
    
    Crie duas classes de exceção que estenda de ExcecaoAlfabeto criada no primeiro exercício, cada uma com o seguinte nome (ExcecaoLetras e ExcecaoNumeros).
    
    Ajuste o programa criado no passo dois criando mais dois métodos, um que valida se a String do parâmetro contém apenas caracteres que são letras, e se não for lance uma exceção do tipo ExcecaoLetra e no segundo valide se a String contém apenas números, em caso negativo lance a exceção ExcecaoNumero.
 */
public class Program {
    
    public static void main(String[] args) {
        
        String nome = "testando";

        if (nome.length()) {
            
        }

        System.out.println(nome.length());
    }
}
